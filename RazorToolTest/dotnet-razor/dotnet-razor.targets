<Project>
  <ItemGroup>
    <DotnetRazor Include="**/*.cshtml" />
  </ItemGroup>
  <Target Name="DotnetRazor" BeforeTargets="CoreGenerateAssemblyInfo">
    <PropertyGroup>
      <RazorCommand>dotnet run --project $(MSBuildThisFileDirectory)dotnet-razor.csproj --</RazorCommand>
    </PropertyGroup>
    <Exec Command="$(RazorCommand) @(DotnetRazor) obj\Razor $(RootNamespace)" ConsoleToMSBuild="true">
      <Output TaskParameter="ConsoleOutput" ItemName="Compile" />
      <Output TaskParameter="ConsoleOutput" ItemName="FileWrites" />
    </Exec>
  </Target>
</Project>